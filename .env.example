DEBUG=True
SECRET_KEY=
ALLOWED_HOSTS=localhost,127.0.0.1
# Set to True only when deployed behind a reverse proxy/load balancer that
# terminates TLS itself and sets X-Forwarded-Proto — never on a box that
# receives traffic directly, since that would let a client spoof "https".
BEHIND_HTTPS_PROXY=False
POSTGRES_DB=iss_db
POSTGRES_USER=iss_user
POSTGRES_PASSWORD=
POSTGRES_HOST=db
POSTGRES_PORT=5432
REDIS_HOST=redis
REDIS_PORT=6379
KOBO_WEBHOOK_SECRET=
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
DEFAULT_FROM_EMAIL=